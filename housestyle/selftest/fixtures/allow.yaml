# Exact file paths only, never wildcards.
allow:
  - path: docs/internal/banned-terms.md
    rules: [no-leverage, no-the-workshop, no-key]
    why: This document names the banned terms so that writers know them.
